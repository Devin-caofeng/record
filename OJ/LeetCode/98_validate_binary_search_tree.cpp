#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include "tree_node.hpp"
using namespace std;

bool isValidBST(TreeNode *root) {
    if (root == NULL) return true;
    return helper(root, )
}

int main() {
    return 0;
}
